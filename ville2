package ProjetRED

import (
	"fmt"
	"strconv"
)
func ville2() {
		var saisie string
	transportdansvilleun := []func(){grotte, jack2}
	fmt.Scanln(&saisie)
	fmt.Println("vous êtes dans la ville 2")
	fmt.Println("appui sur m pour le menu")
	fmt.Println("appui sur j pour aller parler a jack (conseiller avant la forêt)")
	fmt.Println("appui sur f pour aller dans la forêt")
	
	if saisie == "m" {
			menu()
			return
		}
	choixdansvilleun, err := strconv.Atoi(saisie)
	if err != nil || choixdansvilledeux != j || choixdansvilledeux != f {
		fmt.Println("erreur, veuillez entrer une lettre f, j ou m pour le menu")
		continue
	}
	transportdansvilledeux[choixdansvilledeux-1]()
	return
	
}